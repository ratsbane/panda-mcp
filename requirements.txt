# MCP Server
mcp>=1.0.0

# Franka control (panda-python requires libfranka built from source on ARM64)
panda-python>=0.8.0
numpy>=1.24.0

# Camera (use full version, not headless, for GUI support in scene_viewer)
opencv-python>=4.8.0
Pillow>=10.0.0
pyzmq>=25.0.0  # For camera daemon

# Utilities
pydantic>=2.0.0
structlog>=23.0.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
